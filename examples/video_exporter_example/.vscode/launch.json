{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++ Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${file}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      // "windows": {
      // },
      "osx": {
        "MIMode": "lldb",
        "program": "${workspaceRoot}/build/Debug/${workspaceFolderBasename}.app/Contents/MacOS/${workspaceFolderBasename}"
      }
    }
  ]
}
